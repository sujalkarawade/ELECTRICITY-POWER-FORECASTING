{
  "name": "electricity-power-forecasting",
  "version": "1.0.0",
  "description": "Electricity Power Forecasting Web Application using SARIMA Model",
  "scripts": {
    "dev": "concurrently \"python app.py\" \"wait-on http://localhost:5000 && open-cli http://localhost:5000\"",
    "start": "python app.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "open-cli": "^8.0.0",
    "wait-on": "^7.2.0"
  }
}
